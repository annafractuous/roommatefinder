<% object.send(quantified_method).each do |number_value, text| %>
  <% response = object.send(method) %>
  <% checked_value = true if number_value == response.to_i %>
  <%= f.check_box method, {checked: checked_value}, number_value, "0"%>
  <text><%=text%></text>
<% end %>